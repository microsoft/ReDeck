# OpenAI API (default)
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o
# Optional: set for an OpenAI-compatible proxy or local server (vLLM, Ollama, etc.)
OPENAI_BASE_URL=

# Azure OpenAI (optional — overrides above if both are set)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_API_VERSION=2024-12-01-preview
